import { BaseLine } from '../../constants';
declare const _default: ({ root, autoScroll, lineIndex, lines, verticalSpace, }: {
    root: HTMLDivElement | null;
    autoScroll: boolean;
    lineIndex: number;
    lines: BaseLine[];
    verticalSpace: boolean;
}) => void;
export default _default;
